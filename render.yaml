services:
  - type: web
    name: trading-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash -c "python run.py & streamlit run app.py --server.port=10000 --server.address=0.0.0.0"